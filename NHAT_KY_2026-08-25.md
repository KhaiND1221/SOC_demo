# NHẬT KÝ LÀM VIỆC — 2026-08-25

Tóm tắt phiên làm việc hôm nay với Claude (Sonnet 5), để phiên sau (kể cả
chạy ở máy khác) biết đã làm gì, tại sao, và còn thiếu gì.

## 1. Đọc log thật thay vì đoán

Đọc trực tiếp `logs/app/app.log`, `logs/app/auth.log`, `logs/nginx/access.log`,
`logs/nginx/error.log` để hiểu 1 phiên demo thật đã diễn ra (đăng ký/đăng
nhập, CRUD task/comment, IDOR bị chặn 403, crash 500 cố ý, backend chết hẳn
502, brute-force login bị khoá tài khoản) — dùng làm bằng chứng thay vì tin
suông vào nhãn event.

## 2. Chốt lại framework đánh giá của mentor (đã lưu vào memory)

Người dùng đúc kết 1 nguyên tắc gốc + 4 cặp ranh giới khái niệm + 7 kỹ thuật
đặt câu hỏi mà mentor hay dùng (vỏ bọc vs cơ chế thật, công cụ vs kỹ thuật
chung, bằng chứng quan sát vs nguyên nhân thực tế, bước vs mục đích thiết kế).
Đã lưu thành memory `feedback_mentor_evidence_framework` để mọi phiên phân
tích log/demo sau này tự áp dụng lại framework này, không cần nhắc lại.

Đã áp dụng framework đó để soi lại chính các bằng chứng log trong dự án này
(IDOR không có dòng `[DB]`, khoá tài khoản theo IP-OR-username, 500 vs 502
khác lớp trừu tượng...) và sau đó tạo hẳn 1 bộ **16 câu hỏi kiểu mentor** để
luyện tập trước khi quay/thuyết trình (không lưu vào file — chỉ có trong hội
thoại, có thể yêu cầu lại nếu cần).

## 3. Phân tích toàn bộ video demo (`demo_web_2.mp4`, 7 phút 32 giây)

Môi trường lúc đầu **không đọc được video** (không có ffmpeg/công cụ xử lý
video) — đã cài `ffmpeg` (qua `winget install Gyan.FFmpeg`) để xử lý.

Phương pháp: trích 226 khung hình (1 khung/2 giây, có in giờ lên khung hình),
ghép thành 8 ảnh contact-sheet để xem toàn bộ video chỉ qua 8 lượt đọc ảnh
thay vì 226 lượt. Sau đó đối chiếu chéo với:
- **Source code thật** của app (`frontend/*.html`, `frontend/js/api.js`,
  `frontend/css/style.css`, toàn bộ `backend/app/`) — lấy chính xác tuyệt đối
  từng chữ UI, CSS, API, validation, data model.
- **Log thật đúng khung giờ video** — xác nhận `creation_time` của video
  (`2026-08-25T08:52:35Z`, dài 452.47s) khớp tuyệt đối với log thật trong
  đúng cửa sổ đó, nên log này chính là traffic thật của video, không phải suy
  đoán.

Kết quả: file **`DAC_TA_UNG_DUNG_TU_VIDEO.md`** — tài liệu đặc tả cực chi
tiết (15 mục theo đúng khung người dùng yêu cầu: tổng quan, từng trang UI,
auth flow, task CRUD, comment system, user flow từng bước có timestamp, bảng
API, data model, security/logging behavior, error states, timeline 30
giây/đoạn, trích xuất text nguyên văn, spec tổng hợp, acceptance criteria),
đủ để đưa cho 1 AI coding agent khác build lại app giống hệt bản demo. Mọi
nhận định đều gắn nhãn `[OBSERVED]` / `[FROM_SOURCE_CODE]` / `[FROM_LOGS]` /
`[INFERRED]` / `[NOT VISIBLE]` — không bịa chi tiết không có bằng chứng
(ví dụ: trang Profile có trong code nhưng KHÔNG được thao tác trong video
này, đã ghi rõ).

**Giới hạn đã biết**: audio của video không được phân tích (không có công cụ
speech-to-text trong môi trường) — nếu video có lời thuyết minh, nội dung đó
chưa nằm trong tài liệu.

## 4. Đồng bộ lại kịch bản demo với hành vi thật của app

`KICH_BAN_DEMO.md` bị lệch so với app thật sau các lần sửa code gần đây — đã
cập nhật lại:
- Thêm bước xác minh password không lưu plaintext (1.2).
- Thêm bước soi raw Postgres log song song với `watch-logs.ps1` (2.2).
- Thêm bước mới 2.2b (xoá task, đối chiếu DB log).
- Sửa bước IDOR (2.4) để dùng đúng ô "Tra cứu theo ID" trên UI thay vì gọi
  curl thẳng, và sửa lại giải thích: request IDOR **vẫn chạm DB** (SELECT
  thật), chỉ là Postgres không audit SELECT — không phải "bị chặn trước khi
  chạm DB" như bản cũ viết sai.
- Sửa số lần login sai ở bước khoá tài khoản (2.5) từ 4 thành đúng 5, thêm
  ghi chú escape `\"` bắt buộc khi paste JSON qua PowerShell → curl.exe.
  Đổi toàn bộ `http://8080` → `https://8443 -k` cho khớp app đã chuyển sang
  HTTPS mặc định.
- Làm rõ 2.6 (lỗi 500 — route crash không đụng DB) khác bản chất với 2.8
  (bước mới — lỗi 502 do backend container chết hẳn, đối chiếu `error.log`
  "Host is unreachable").

## 5. Chạy thử app

`docker compose up -d --build` — cả 3 container (db/backend/nginx) lên khoẻ
mạnh, `GET /api/health` trả `{"status":"ok"}`, `https://localhost:8443` trả
200.

## 6. Commit (đã tạo, CHƯA push xong)

2 commit đã tạo trên `master`:
- `1d44769` — đồng bộ `KICH_BAN_DEMO.md` với hành vi app thật + whitespace
  nhỏ ở `frontend/index.html`.
- `f6f3c48` — thêm `DAC_TA_UNG_DUNG_TU_VIDEO.md`.

`git push origin master` **bị chặn bởi permission classifier** của Claude
Code (hành động ảnh hưởng remote/shared state cần xác nhận thủ công) — cần
người dùng tự chạy `git push origin master` hoặc xác nhận rõ ràng trong phiên
chat để Claude push hộ.

## Việc còn dang dở / cần làm tiếp

- [ ] Push 2 commit trên lên `origin/master` (hoặc file này + commit mới nếu
      được tạo sau khi đọc nhật ký này).
- [ ] Cân nhắc bổ sung phần audio/thuyết minh của video (cần công cụ
      speech-to-text, hiện chưa có trong môi trường).
- [ ] Nếu quay lại video demo lần sau, có thể thao tác luôn cả Phần 1.4 (hồ
      sơ cá nhân — đổi email) vì lần quay này chưa có bằng chứng cho phần đó.
